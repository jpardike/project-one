<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <%- include('../partials/head-contents') %>
    <!-- p-edit CSS -->
    <link rel="stylesheet" href="/styles/p-edit.css">
  </head>
  <body>

    <header class="row justify-content-center">
      <h1>Update Listing</h1>
    </header>

    <%- include('../partials/navbar') %>

    <form class="edit-box" action="/products/<%= product.id %>?_method=PUT" method="POST">
      <div class="input-containers">
        <label for="type">For Sale: </label>
        <input class="text-input" type="text" name="type" value="<%= product.type %>" />
      </div>

      <div class="input-containers">
        <label for="price">Price: </label>
        <input class="text-input" type="text" name="price" value="<%= product.price %>" />
      </div>

      <div class="input-containers">
        <label for="condition">Condition: </label>
        <input class="text-input" type="text" name="condition" value="<%= product.condition %>" />
      </div>

      <div class="input-containers">
        <label for="image">Image: </label>
        <input class="text-input" type="text" name="image" value="<%= product.image %>" />
      </div>

      <input class="btn mt-3" type="submit" />
    </form>

    <%- include('../partials/footer') %> 
    
  </body>
</html>