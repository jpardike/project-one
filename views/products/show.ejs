<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <%- include('../partials/head-contents') %>
    <!-- p-show CSS -->
    <link rel="stylesheet" href="/styles/p-show.css">
  </head>
  <body>

    <header>
      <h1 class="text-center">Product For Sale</h1>
    </header>

    <%- include('../partials/navbar') %> 
    <%- include('../partials/searchbar') %> 
    
    <main class="row product-container justify-content-start">
      <div class="col">
        <div class="row">
          <a class="p-3 product-text user-name-link mt-5" href="/users/<%= product.user.id %>"><%= product.user.userName %> </a>   
          <p class="p-3 product-text mt-5"><%= product.type %> </>
          <p class="p-3 product-text mt-5"> $ <%= product.price %> </p>
          
        </div>
    
        <img class="product-image mt-5" src="<%= product.image %>" alt="" />
        <p class="product-text">Condition: <%= product.condition %> </p>

        <form class="show-product-form" action="/products/<%= product.id %>?_method=DELETE" method="POST">
          <input class="btn mr-2 mt-5" type="submit" value="Delete">
        </form>

        <form class="show-product-form" action="/products/<%= product.id %>/edit" method="GET">
          <input class="btn mt-5" type="submit" value="Update">
        </form>
      </div>

      <div class="col-6 justify-content-center text-center">
        <p class="location product-text">Location: <%= product.user.location %> </p>

        <!-- Google Maps Embedded API -->
        <iframe
          class="show-map"
          frameborder="0" style="border:0"
          src="https://www.google.com/maps/embed/v1/place?key=<%= api %> 
            &q=<%= product.user.location %>" allowfullscreen>
        </iframe>
      </div>

    </main>

    <%- include('../partials/footer') %> 
  </body>
</html>